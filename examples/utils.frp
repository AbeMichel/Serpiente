depuis math_lib importer sumar

definir saluer(nom):
    afficher(f"Bonjour, {nom}!")

definir calculer_total(prix, quantite):
    total = sumar(prix, 0) # Just using sumar from Spanish for demo
    retourner total * quantite

